@import 'functions'

// TODO: Move constants into hashes.
// @type String
ARROW_SIZE_MENU = 10px

// @type String
ARROW_SIZE_TOGGLE = 5px

// @type Hash
Buttons = {
  HEIGHT: px_to_rem(40)
  ROW_HEIGHT: px_to_rem(96)
}

// @type Hash
Cards = {
  CORNER_RADIUS: 8px
  PADDING: px_to_rem(24)
}

// @type Hash
Grid = {
  Base: {
    COLUMN_GAP: px_to_rem(16)
    ROW_GAP: px_to_rem(16)
  }
  Medium: {
    COLUMN_GAP: px_to_rem(24)
    ROW_GAP: px_to_rem(24)
  }
}

// @type Hash
Header = {
  Base: {
    HEIGHT: px_to_rem(44)  
  }
  Medium: {
    HEIGHT: px_to_rem(60)
  }
}

// @type Hash
Inputs = {
  HEIGHT: px_to_rem(32)
  WIDTH: px_to_rem(88)
}

// @type Hash
Menu = {
  CORNER_RADIUS: 4px
  ITEM_SIZE: px_to_rem(40)
  PADDING: px_to_rem(16)
}

// @type hash
Tables = {
  MAX_WIDTH: px_to_rem(640)
  PADDING_X: px_to_em(12)
  PADDING_X_LARGE: px_to_em(16)
  PADDING_Y: px_to_em(8)
}

// @type Hash
Transitions = {
  DEFAULT: .3s
  DRAWER: .5s
}

// @type String
Values = {
  MAX_WIDTH: px_to_rem(240)
}

// -----------------------------------------------------------------------------
// Compound dimensions
// @type String
VALUES_GRID_WIDTH = Values.MAX_WIDTH + (2 * Cards.PADDING)

// @type String
CONTENT_WIDTH_LARGE = (Values.MAX_WIDTH + (2 * Cards.PADDING)) + Tables.MAX_WIDTH + Grid.Medium.COLUMN_GAP

// -----------------------------------------------------------------------------
// Breakpoints
// @type Hash
Breakpoints = {
  SMALL: 'screen and (min-width: %s)' % (px_to_rem(420))
  MEDIUM: 'screen and (min-width: %s)' % (Tables.MAX_WIDTH + (2 * Grid.Medium.COLUMN_GAP))
  LARGE: 'screen and (min-width: %s)' % (CONTENT_WIDTH_LARGE + (2 * Grid.Medium.COLUMN_GAP))
}