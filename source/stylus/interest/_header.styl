.header
  background GREEN
  color white
  height '%s' % HeaderDimensions.Base.HEIGHT
  left 0
  position fixed
  right 0
  transform translate3d(0, 0, 0)
  transition transform TRANSITION_SPEED ease
  top 0
  z-index 1

  @media Breakpoints.MEDIUM
    height '%s' % HeaderDimensions.Medium.HEIGHT

.header.unpinned
  transform translate3d(0, -100%, 0)

.header__frame
  align-items center
  display flex
  height '%s' % HeaderDimensions.Base.HEIGHT
  justify-content space-between
  margin 0 auto
  padding 0 COLUMN_GAP

  @media Breakpoints.MEDIUM
    height '%s' % HeaderDimensions.Medium.HEIGHT
    padding 0 COLUMN_GAP_MEDIUM

  @media Breakpoints.LARGE
    max-width CONTENT_WIDTH_LARGE
    padding 0

.header__title
  font-size '%s' % FontSizes.Base.HEADER
  font_weight(600)
  line-height 1
  margin 0
  white-space nowrap

  @media Breakpoints.MEDIUM
    font-size '%s' % FontSizes.Medium.HEADER
