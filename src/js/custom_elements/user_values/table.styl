.table
  box-sizing border-box
  font-variant-numeric tabular-nums // Align , and . across cells.
  grid-area table
  height auto
  justify-self stretch
  margin 0
  opacity 1
  overflow-x auto
  overflow-y hidden
  text-align center
  transition height transition-slow ease-out, opacity transition-slow ease-out

  @media breakpoint.large
    height auto !important // Always display on large screens and up.
    justify-self stretch
    opacity 1
    padding 0

.table[hidden]
  display none

.table__frame
  border-bottom 1px solid var(--border)
  overflow-x auto

  @media breakpoint.small
    border 1px solid var(--border)
    border-radius card-corner-radius

@media breakpoint.small
  table
    background var(--background)

th
td
  padding var(--table-padding-y) var(--table-padding-x)

  &:first-child
    text-align left
    padding-left grid-gap

    @media breakpoint.small
      padding-left var(--table-padding-x)

  &:last-child
    padding-right grid-gap

    @media breakpoint.small
      padding-right var(--table-padding-x)

  &.balance
    padding-right grid-gap

    @media breakpoint.small
      padding-right var(--table-padding-x)

thead > tr
  border-bottom 1px solid var(--border)

tbody > tr:nth-child(odd)
  background var(--zebra-stripe)

td.growth::after
td.deposits::before
td.interest::before
td.balance::before
  color var(--symbol)

td.growth::after
  content '%'
  margin-left em(4)

.year
  padding-right 0

.growth
.interest
  display none

  @media breakpoint.small
    display table-cell

.rotate-screen
  color var(--symbol)
  display none
  font-size var(--fineprint)
  margin rem(12) grid-gap 0

  @media screen and (max-width breakpoint-small-units) and (orientation portrait)
    display block

.rotate-screen span
  color var(--text)

[no-js] .table
  display none