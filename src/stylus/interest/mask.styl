.mask
  background rgba(black, .8)
  bottom 0
  left 0
  opacity 0
  position fixed
  right 0
  top 0
  transition opacity var(--transition), visibility var(--transition)
  visibility hidden
  z-index -1

.settings[open] .mask
  opacity 1
  visibility visible
